name: Build Türklion Launcher (All Platforms)

on:
  push:
    branches: [main, master]
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    name: Windows Build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Windows
        run: npx electron-builder --win --publish never

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: TurklionLauncher-Windows
          path: dist/*.exe
          retention-days: 30

  build-linux:
    name: Linux Build
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y rpm fakeroot dpkg libarchive-tools

      - name: Install dependencies
        run: npm install

      - name: Build Linux
        run: npx electron-builder --linux AppImage deb --publish never

      - name: Upload Linux AppImage
        uses: actions/upload-artifact@v4
        with:
          name: TurklionLauncher-Linux-AppImage
          path: dist/*.AppImage
          retention-days: 30

      - name: Upload Linux deb
        uses: actions/upload-artifact@v4
        with:
          name: TurklionLauncher-Linux-deb
          path: dist/*.deb
          retention-days: 30

  build-macos:
    name: macOS Build
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build macOS
        run: npx electron-builder --mac dmg --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: TurklionLauncher-macOS
          path: dist/*.dmg
          retention-days: 30

  release:
    name: Create Release
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: List files
        run: find release-files -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "Turklion Launcher ${{ github.ref_name }}"
          body: |
            ## Turklion Launcher ${{ github.ref_name }}

            ### İndirme Linkleri
            | Platform | Dosya | Açıklama |
            |----------|-------|----------|
            | Windows | `.exe` | Kurulum gerektirmez, direkt çalıştır |
            | Linux | `.AppImage` | `chmod +x` sonra çalıştır |
            | Linux | `.deb` | `dpkg -i` ile kur |
            | macOS | `.dmg` | Aç ve Applications'a sürükle |
          files: release-files/**/*
          draft: false
          prerelease: false
